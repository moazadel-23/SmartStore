@{
    Layout = "~/Views/Shared/_AuthLayout.cshtml";
    ViewData["Title"] = "Forgot Password";
}

<!-- Forgot Password Navigation Header -->
<div class="auth-nav-header">
    <a asp-area="Identity" asp-controller="Account" asp-action="Login" class="back-btn-circle" title="Back to Sign In">
        <i class='bx bx-chevron-left'></i>
    </a>
</div>

<div class="auth-form-header">
    <h2>Forgot Password?</h2>
    <p>No worries! Enter your email and we'll send you a reset code.</p>
</div>

<form asp-area="Identity" asp-controller="Account" asp-action="ForgotPassword" method="post" id="forgotForm" novalidate class="auth-form">
    @Html.AntiForgeryToken()

    <div asp-validation-summary="ModelOnly" class="auth-alert alert-error"></div>

    <!-- Email Field -->
    <div class="auth-field">
        <label for="ForgotEmail" class="field-label">Email Address</label>
        <div class="input-wrap">
            <input type="email" id="ForgotEmail" name="Email" class="auth-input" placeholder="sarkarraj0766@gmail.com" autocomplete="email" required>
        </div>
        <span class="field-error" id="err-forgot-email"></span>
    </div>

    <!-- Submit Button -->
    <button type="submit" class="auth-submit-btn" id="forgotSubmitBtn">
        <span id="forgotLabel">Continue</span>
        <div class="btn-spinner" id="forgotSpinner"></div>
    </button>
</form>

<div class="auth-footer-note">
    Remember your password? <a asp-area="Identity" asp-controller="Account" asp-action="Login">Sign in</a>
</div>

@section Scripts {
<script>
(function () {
    function showErr(id, msg) {
        const el = document.getElementById(id);
        if (!el) return;
        el.innerHTML = msg ? `<i class='bx bx-error-circle'></i> ${msg}` : '';
    }

    document.getElementById('forgotForm')?.addEventListener('submit', function (e) {
        const em = document.getElementById('ForgotEmail');
        const emailRe = /^[^\s@@]+@@[^\s@@]+\.[^\s@@]+$/;
        if (!em?.value.trim() || !emailRe.test(em.value)) {
            e.preventDefault();
            showErr('err-forgot-email', 'Please enter a valid email address');
            em?.classList.add('input-error');
            return;
        }
        showErr('err-forgot-email', '');
        em?.classList.remove('input-error');
        
        const btn = document.getElementById('forgotSubmitBtn');
        const lbl = document.getElementById('forgotLabel');
        const sp  = document.getElementById('forgotSpinner');
        if (btn) btn.disabled = true;
        if (lbl) lbl.style.display = 'none';
        if (sp)  sp.style.display = 'block';
    });
})();
</script>
}
